#include "karnaughmap.h"
#include <cmath>

bool KarnaughMap::init(std::vector<char> inputLetters)
{
    int xInputsCount = std::floor(inputLetters.size() / 2);
    int yInputsCount = inputLetters.size() - xInputsCount;
}
