BIG TODO: cloning nodes needs to be managed in terms of linking. Also clearing backups without deleting is wasting memory.

bool GateFPGA::UpdateClicked(int clickX, int clickY)
{
    bool ret = Gate::UpdateClicked(clickX, clickY);
    if(ret)
    {
        if(m_pParentField->GetCurrentClickMode() == CLICK_DEFAULT)
        {
            OpenEditor();
        }
    }

    return ret;
}


m_bIsNested could just be determined from if m_pParentGateCollection is nullptr right?

get m_bEnabled in updateoutput